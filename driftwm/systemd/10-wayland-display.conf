# The real session's socket is always wayland-1 (first socket at boot).
# Pinning it here makes DMS startup independent of the compositor's
# import-environment, which has failed and cost the shell at boot before.
# The unit's own ExecStartPre still waits for the socket to actually exist.
[Service]
Environment=WAYLAND_DISPLAY=wayland-1
