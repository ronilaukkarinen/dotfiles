-- Copy this file to secrets.lua and fill in your actual values
-- secrets.lua is gitignored and won't be committed

local M = {}

function M.apply(config)
  -- Add any secret or machine-specific wezterm settings here
  -- Example: config.some_api_key = "your_key"
end

return M
